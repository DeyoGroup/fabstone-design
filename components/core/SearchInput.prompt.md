Frosted search bar for the dark inventory sections. Pairs with Pill tabs in a `.fs-collections__controls` row.

```jsx
const { SearchInput } = window.FabstoneDesignSystem_e3db74;
<SearchInput value={q} onChange={setQ} />
```

Placeholder copy is functional and specific ("Search by stone type, color or application...").
