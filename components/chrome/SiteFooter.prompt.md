The site footer — closes every website screen. Defaults carry the real site's columns and copy.

```jsx
const { SiteFooter } = window.FabstoneDesignSystem_e3db74;
<SiteFooter logo="assets/images/logo-fabstone.png" />
```
