The closing red banner — last section before the footer on marketing pages.

```jsx
const { CtaBanner } = window.FabstoneDesignSystem_e3db74;
<CtaBanner onPrimary={() => go('Contact')} onSecondary={() => go('Inventory')} />
```

Headline is a Title Case question; buttons: white primary + ghost secondary.
